import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginBottom: 20,
  },
  title: {
    color: '#000',
  },
  textInput: {
    borderWidth: 1,
    borderColor: '#000',
    padding: 10,
    marginTop: 10,
    borderRadius: 10,
  },
});

export default styles;
